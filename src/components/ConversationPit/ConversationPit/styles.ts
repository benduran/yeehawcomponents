import { css } from '@emotion/css';

export const styles = {
  /**
   * Classname applied to the root, outer-most <div />
   * that contains the <ConversationPit /> component
   */
  root: css`
    display: flex;
    flex-direction: column;
    min-height: 0;
  `,
};
